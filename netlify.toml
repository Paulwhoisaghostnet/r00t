# Netlify build for r00t wallet summary app
# Connect this repo in Netlify and set build command to run from app/
[build]
  base = "app"
  command = "npm ci && npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
