# Secret key for Nimrod's wallet (edsk...). Never commit the real value.
NIMROD_SECRET_KEY=

# Optional: bearer token required for POST /transfer and GET /balance (Authorization: Bearer <token>)
SIGNER_AUTH_TOKEN=

# Optional: Tezos RPC URL (default: mainnet)
RPC_URL=https://mainnet.api.tez.ie

# Optional: port (default 3333)
PORT=3333
